# BHUMI-SETU

Intelligent Land Record Digitization and Validation System.

> **For AI assistants:** read `CLAUDE.md` first, then `docs/HANDOFF.md`.
> This README is the original project plan; `docs/` tracks what's
> actually built, phase by phase.

## Project

BHUMI-SETU provides a FastAPI backend, a React frontend, PostgreSQL with PostGIS, Redis, MinIO-compatible object storage, and Keycloak services managed through Docker Compose.